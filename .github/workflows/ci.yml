name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: gradle

    - name: Make Gradle wrapper executable
      run: chmod +x ./gradlew

    - name: Lint Check
      run: ./gradlew lint --no-daemon --scan

    - name: Build Debug APK
      run: ./gradlew assembleDebug --no-daemon

    - name: Run Unit Tests
      run: ./gradlew testDebugUnitTest --no-daemon

    - name: Unit Test Coverage (JaCoCo)
      if: always()
      run: ./gradlew createDebugUnitTestCoverageReport --no-daemon

    - name: Upload Test Results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: '**/build/test-results/**'
        retention-days: 30

    - name: Upload Coverage Reports
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: coverage-reports
        path: 'app/build/reports/coverage/**'
        retention-days: 30

    - name: Publish Test Report
      if: always()
      uses: dorny/test-reporter@v1
      with:
        name: Unit Test Results
        path: '**/build/test-results/testDebugUnitTest/**.xml'
        reporter: 'java-junit'
        fail-on-error: false

    - name: Comment PR with Build Status
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        script: |
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: '✅ Build and Tests Completed\n\n- ✓ Lint Check Passed\n- ✓ Debug APK Built\n- ✓ Unit Tests Passed\n- ✓ Coverage Report Generated (JaCoCo)'
          })
      continue-on-error: true

  instrumented-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: gradle

    - name: Make Gradle wrapper executable
      run: chmod +x ./gradlew

    - name: Run Connected Tests
      uses: reactivecircus/android-emulator-runner@v2
      with:
        api-level: 33
        arch: x86_64
        force-avd-creation: false
        emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
        disable-animations: true
        script: ./gradlew connectedAndroidTest --no-daemon

    - name: Upload Instrumented Test Results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: instrumented-test-results
        path: '**/build/outputs/androidTest-results/**'
        retention-days: 30

  code-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: gradle

    - name: Make Gradle wrapper executable
      run: chmod +x ./gradlew

    # Non-blocking until the first cleanup pass (./gradlew ktlintFormat, then fix or baseline detekt findings).
    - name: Ktlint Check
      run: ./gradlew ktlintCheck --no-daemon
      continue-on-error: true

    - name: Detekt Analysis
      run: ./gradlew detekt --no-daemon
      continue-on-error: true

    - name: Upload Analysis Results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: code-quality-reports
        path: '**/build/reports/**'
        retention-days: 30
